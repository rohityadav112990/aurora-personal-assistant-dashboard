import React from 'react';
import AnalyticsPanel from '../components/AnalyticsPanel';

const Analytics = ({ accentColor }) => {
  return <AnalyticsPanel accentColor={accentColor} />;
};

export default Analytics;
