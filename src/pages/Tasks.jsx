import React from 'react';
import TaskManager from '../components/TaskManager';

const Tasks = ({ accentColor }) => {
  return <TaskManager accentColor={accentColor} />;
};

export default Tasks;