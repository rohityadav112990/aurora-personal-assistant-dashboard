import React from 'react';
import AiChatPanel from '../components/AiChatPanel';

const Assistant = ({ accentColor }) => {
  return <AiChatPanel accentColor={accentColor} />;
};

export default Assistant;